# Focus - منصة التعاون الشاملة

تطبيق ويب شامل لإدارة المشاريع والتعاون في الفريق مع دعم اللغة العربية كاملاً.

## المميزات 🚀

- ✅ إدارة المشاريع والمهام
- ✅ لوحة كانبان ديناميكية
- ✅ غرفة محادثة مباشرة (Real-time Chat)
- ✅ لوحة بيضاء تعاونية (Whiteboard)
- ✅ نظام المنشورات والتعليقات
- ✅ إدارة الملفات
- ✅ نظام الإخطارات المتقدم
- ✅ تكامل الذكاء الاصطناعي (Claude AI)
- ✅ دعم المشاريع العامة مع طلبات الانضمام
- ✅ Dark/Light Mode (أسود/أخضر في الليل، أبيض/أزرق في النهار)
- ✅ دعم كامل للغة العربية

## المتطلبات ⚙️

### Windows
- **Node.js** (الإصدار 16 أو أحدث) - [تحميل من هنا](https://nodejs.org/)
- **PostgreSQL** (الإصدار 12 أو أحدث) - [تحميل من هنا](https://www.postgresql.org/download/windows/)
- **npm** (يأتي مع Node.js)

### macOS/Linux
- Node.js
- PostgreSQL

## خطوات التثبيت 📦

### 1. استنساخ المشروع أو فتح المجلد

```bash
# إذا كنت تريد استنساخه
git clone <repository-url>
cd focus-app

# أو إذا كان المجلد موجوداً
cd <project-folder>
```

### 2. تثبيت المكتبات

```bash
npm install
```

### 3. إعداد قاعدة البيانات

#### على Windows (استخدم PowerShell أو Command Prompt)

1. **فتح PostgreSQL**
   - ابحث عن "SQL Shell (psql)" في Windows
   - أو افتح Command Prompt واكتب:
   ```bash
   psql -U postgres
   ```

2. **إنشاء قاعدة البيانات**
   ```sql
   CREATE DATABASE focus_app;
   \q
   ```

3. **إعداد ملف `.env`**
   ```bash
   # انسخ الملف
   copy .env.example .env
   
   # ثم عدّل البيانات الخاصة بك في .env
   ```

   مثال على `.env`:
   ```
   DATABASE_URL=postgresql://postgres:password@localhost:5432/focus_app
   PGHOST=localhost
   PGPORT=5432
   PGDATABASE=focus_app
   PGUSER=postgres
   PGPASSWORD=your_postgres_password
   SESSION_SECRET=your-random-secret-key-12345
   ANTHROPIC_API_KEY=your-anthropic-key
   ```

4. **مزامنة قاعدة البيانات**
   ```bash
   npm run db:push
   ```

### 4. التشغيل 🎮

```bash
npm run dev
```

سيظهر الرابط في الـ Terminal:
```
[express] serving on port 5000
```

افتح المتصفح واذهب إلى: **http://localhost:5000**

## ملفات الكود الرئيسية 📁

```
focus-app/
├── client/                    # الواجهة الأمامية (React)
│   └── src/
│       ├── pages/            # صفحات التطبيق
│       ├── components/       # مكونات React
│       └── App.tsx           # تطبيق رئيسي
├── server/                   # الخادم (Express)
│   ├── routes.ts            # جميع الـ API Endpoints
│   ├── storage.ts           # منطق قاعدة البيانات
│   └── index-dev.ts         # خادم التطوير
├── shared/                  # الكود المشترك
│   └── schema.ts            # تعريف نماذج البيانات
├── .env.example             # مثال على متغيرات البيئة
└── package.json             # المكتبات والإعدادات
```

## المتغيرات البيئية 🔐

يجب نسخ `.env.example` إلى `.env` وتعبئة البيانات الخاصة بك:

| المتغير | الوصف | مثال |
|--------|-------|------|
| `DATABASE_URL` | رابط قاعدة البيانات | `postgresql://user:pass@localhost:5432/focus_app` |
| `SESSION_SECRET` | مفتاح سري للجلسات | `your-random-key-here` |
| `ANTHROPIC_API_KEY` | مفتاح Anthropic Claude | احصل عليه من [هنا](https://console.anthropic.com) |
| `PGHOST` | عنوان PostgreSQL | `localhost` |
| `PGPORT` | منفذ PostgreSQL | `5432` |
| `PGDATABASE` | اسم قاعدة البيانات | `focus_app` |
| `PGUSER` | اسم المستخدم | `postgres` |
| `PGPASSWORD` | كلمة السر | كلمتك السرية |

## الأوامر المتاحة 🛠️

```bash
# تشغيل في وضع التطوير
npm run dev

# بناء للإنتاج
npm run build

# تشغيل الإنتاج
npm start

# فحص الأخطاء
npm run check

# مزامنة قاعدة البيانات
npm run db:push
```

## استكشاف الأخطاء 🐛

### خطأ: "Database connection failed"
- تأكد من تشغيل PostgreSQL
- تحقق من بيانات الاتصال في `.env`
- تأكد من وجود قاعدة البيانات `focus_app`

### خطأ: "PORT 5000 is already in use"
- استخدم منفذ مختلف بتعديل `server/index-dev.ts`
- أو أغلق البرنامج الذي يستخدم المنفذ

### خطأ: "Cannot find module"
- احذف `node_modules` و `package-lock.json`
- شغّل `npm install` من جديد

### خطأ: في PostgreSQL على Windows
1. افتح Services (اضغط Windows + R وكتب `services.msc`)
2. ابحث عن `PostgreSQL`
3. تأكد أنه مفعّل (Status = Started)

## الحسابات الافتراضية 👤

عند التشغيل لأول مرة:

| البريد | كلمة السر | الدور |
|-------|---------|------|
| `hamza.helnady@gmail.com` | `123456` | Admin |

**ملاحظة**: غيّر هذه البيانات فوراً عند الاستخدام في الإنتاج!

## الدعم 💬

للمزيد من المساعدة، تواصل معنا أو افتح issue على GitHub.

## الترخيص 📜

This project is created by حمزة هشام أحمد النادي

---

**استمتع باستخدام Focus! 🎉**
