# شرح خطوة بخطوة لتشغيل Focus على Windows 🪟

## الخطوة 1️⃣: تثبيت Node.js

1. اذهب إلى [nodejs.org](https://nodejs.org/)
2. حمّل **LTS version** (الإصدار الموصى به)
3. شغّل الملف وانقر Next حتى الانتهاء
4. افتح Command Prompt وتحقق:
   ```bash
   node --version
   npm --version
   ```

## الخطوة 2️⃣: تثبيت PostgreSQL

1. اذهب إلى [postgresql.org/download/windows](https://www.postgresql.org/download/windows/)
2. حمّل أحدث إصدار
3. شغّل الملف وتابع التثبيت:
   - اضغط Next
   - ذاكرة التخزين: قبول الافتراضي
   - كلمة السر: **احفظها!** (مثلاً: `password`)
   - Port: **5432** (افتراضي)
   - اضغط Next ثم Finish

4. **تحقق من التثبيت**: ابحث عن "SQL Shell (psql)" في Windows وشغّله

## الخطوة 3️⃣: إنشاء قاعدة البيانات

1. افتح **SQL Shell (psql)** من Windows
2. اضغط Enter 4 مرات (كل استفسار)
3. أدخل كلمة السر التي وضعتها
4. اكتب:
   ```sql
   CREATE DATABASE focus_app;
   ```
5. اكتب:
   ```sql
   \q
   ```

## الخطوة 4️⃣: فتح المشروع

1. افتح **File Explorer** (مستكشف الملفات)
2. اذهب لمجلد المشروع
3. اضغط يمين على المجلد وانقر **"Open PowerShell window here"**
   - أو افتح Command Prompt وكتب: `cd path\to\project`

## الخطوة 5️⃣: إعداد البيئة

في PowerShell أو Command Prompt:

```bash
# تثبيت المكتبات
npm install

# انسخ ملف الإعدادات
copy .env.example .env
```

## الخطوة 6️⃣: تعديل ملف .env

1. افتح `.env` بـ Notepad
2. ارجع إلى كلمة السر PostgreSQL التي وضعتها في الخطوة 2
3. عدّل السطر:
   ```
   PGPASSWORD=password
   ```
   اكتب كلمتك بدلاً من `password`

4. احفظ الملف (Ctrl + S)

## الخطوة 7️⃣: مزامنة قاعدة البيانات

في نفس PowerShell:

```bash
npm run db:push
```

انتظر حتى الانتهاء

## الخطوة 8️⃣: التشغيل! 🚀

```bash
npm run dev
```

ستظهر رسالة:
```
[express] serving on port 5000
```

افتح متصفحك واذهب إلى: **http://localhost:5000**

## حسابات الدخول 👤

```
البريد: hamza.helnady@gmail.com
كلمة السر: 123456
```

---

## مشاكل شائعة وحلولها ⚠️

### ❌ خطأ: "npm: command not found"
**✅ الحل**: أعد تثبيت Node.js وأعد تشغيل Command Prompt

### ❌ خطأ: "Database connection failed"
**✅ الحل**:
- تأكد من تشغيل PostgreSQL
- ابحث عن "Services" في Windows
- ابحث عن "postgresql"
- تأكد أنه "Running"

### ❌ خطأ: "Port 5000 already in use"
**✅ الحل**: استخدم منفذ مختلف أو أغلق البرنامج الآخر

### ❌ خطأ: في PostgreSQL Password
**✅ الحل**: أعد تثبيت PostgreSQL بكلمة سر تتذكرها (بدون رموز خاصة)

---

## ملخص الأوامر الأساسية 📝

```bash
# التثبيت الأول
npm install
npm run db:push

# التشغيل اليومي
npm run dev

# البناء للإنتاج
npm run build
npm start
```

---

**لديك أي مشكلة؟ أعد قراءة هذه الخطوات بعناية! 😊**
